<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="preferenceTheme">@style/Theme.MyTheme.PreferenceThemeOverlay</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="drawerArrowStyle">@style/Theme.MyTheme.DrawerArrowStyle</item>
        <item name="toolbarStyle">@style/Theme.MyTheme.Toolbar</item>
        <item name="dialogTheme">@style/Theme.MyTheme.Dialog</item>
        <item name="alertDialogTheme">@style/Theme.MyTheme.Dialog.Alert</item>
        <!-- Overriding the category views as they don't look good -->
        <item name="android:listSeparatorTextViewStyle">@style/Theme.MyTheme.ListSeparatorTextView</item>
    </style>

    <style name="Theme.MyTheme.DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="Theme.MyTheme.Toolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="color">@android:color/white</item>
        <item name="titleTextAppearance">@style/Theme.MyTheme.Toolbar.TextAppearance</item>
        <item name="subtitleTextAppearance">@style/Theme.MyTheme.Toolbar.TextAppearance.Subtitle
        </item>

    </style>

    <style name="Theme.MyTheme.Toolbar.TextAppearance" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Theme.MyTheme.Toolbar.TextAppearance.Subtitle" parent="@style/TextAppearance.Widget.AppCompat.Toolbar.Subtitle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Theme.MyTheme.Dialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.MyTheme.Dialog.Alert" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/accent</item>
    </style>

    <!-- Overriding preference theme to eliminate left-right padding -->
    <!-- Actually it would require one to provide the custom layout files with the padding INSIDE
    the preference layouts, not on the preference list itself, thus, I'll not do this now

    <style name="PreferenceThemeOverlayFix" parent="@style/PreferenceThemeOverlay">
        <item name="preferenceFragmentListStyle">@style/PreferenceFragmentListFix</item>
    </style>

    <style name="PreferenceFragmentListFix">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
    </style>-->

    <!-- The default category looks bad, so overriding its appearance now -->
    <style name="Theme.MyTheme.ListSeparatorTextView">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/accent</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="Theme.MyTheme.PreferenceThemeOverlay" parent="@style/PreferenceThemeOverlay">
        <item name="preferenceFragmentListStyle">@style/Theme.MyTheme.PreferenceFragmentList</item>
    </style>
    <style name="Theme.MyTheme.PreferenceFragmentList" parent="@style/PreferenceFragmentList">
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbars">vertical</item>
    </style>

</resources>
